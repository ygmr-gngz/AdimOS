# ── Web Framework ─────────────────────────────────────────────
fastapi==0.115.6
uvicorn[standard]==0.32.1
python-multipart==0.0.19

# ── Supabase ──────────────────────────────────────────────────
supabase==2.10.0
httpx==0.27.2

# ── OpenAI ───────────────────────────────────────────────────
# 1.57.4 -> 2.52.0: gpt-image-2/gpt-image-1-mini için gerekli (images.generate
# eski sürümde quality='low'/'medium', output_format, size=1024x1536 kabul
# etmiyordu — TypeError). Doğrulandı: tek breaking change 2.0.0'da Responses
# API'ye özel (bu kod tabanında hiç kullanılmıyor); chat.completions.create,
# audio.speech.create, audio.transcriptions.create (tüm gerçek kullanım
# noktaları) imzaları değişmeden duruyor.
openai==2.52.0

# ── Google Gemini ────────────────────────────────────────────
google-generativeai==0.8.3

# ── PDF & Doküman İşleme ──────────────────────────────────────
pypdf==5.1.0
cryptography>=3.1
python-docx==1.1.2
tiktoken==0.8.0

# ── İçerik & Video Üretimi ────────────────────────────────────
num2words==0.5.14
moviepy==1.0.3
Pillow==11.1.0
numpy==1.26.4
mutagen==1.47.0

# ── Ortam & Config ────────────────────────────────────────────
python-dotenv==1.0.1
pydantic==2.10.3
pydantic-settings==2.6.1

# ── Sosyal Medya (İçerik Otomasyonu) ─────────────────────────
google-api-python-client==2.155.0
google-auth==2.37.0
google-auth-oauthlib==1.2.1
# instagram-graph-api için requests yeterli (Meta Graph API HTTP üzerinden)
requests==2.32.3

# ── Görev Kuyruğu / Zamanlama ─────────────────────────────────
apscheduler==3.10.4

# ── Logging & Monitoring ─────────────────────────────────────
loguru==0.7.3

# ── Matematik Doğrulama (Section 4) ──────────────────────────
sympy==1.13.3

# ── Test ─────────────────────────────────────────────────────
pytest==8.3.4
pytest-asyncio==0.24.0
